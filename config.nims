# https://nim-lang.github.io/Nim/nimc.html#compiler-usage-compileminustime-symbols
# Another common use is the -d:ssl switch to activate SSL sockets.
# --define: "ssl"

# --out: "out/"
# --verbosity: 0
switch("out", "/tmp/")
